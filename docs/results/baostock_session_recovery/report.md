# BaoStock用户续采会话失败修复

2026-09-07 12:34:06 UTC，第100项`sh.600252 / query_performance_express_report`返回10001001，SDK常量明确为BSERR_NO_LOGIN“用户未登录”。前99项成功、94行数据保留；当前剩591项。这不是此前10001011黑名单的重现；未确认会话丢失的具体原因，不能断定一定是超时或另一客户端挤下线。JSON原始消息正常，用户终端存在中文编码显示问题。

修复提交`3da1cec`。冻结采集器与数据定义不变；用户执行入口`resume_r2_baostock_events.py`按新session recovery配置处理明确无数据的10001001：先保存失败响应、原状态、登录记录及manifest，再重新登录，由旧采集器从失败槽位继续。成功响应不删除、不重取，原错误不擦除。每失败项最多一次重登重发，全批最多60次，重登前等待5秒；黑名单、通信未知、首次登录失败、同项重复失效或超预算均停止。

八项针对性测试通过：只准10001001空响应、黑名单及未知响应拒绝、同项和累计预算限制、原失败及成功数据保全、模拟进程接续，以及已有事件时间/范围边界测试。`--check`核对99个成功断点及全量源文件manifest通过，未发网络请求。实际重新登录并补发第100项尚待用户运行，不能把模拟测试当作服务恢复证明。

状态仍为STOPPED；采集与自动归档进程均未由助手重启。用户使用同一续采入口，PowerShell设为UTF-8并追加保存日志；完成后通知助手做最终数据核验。此修复不增加因子评价、不改旧来源、不打开保护期。

原始[失败响应](failed_response.json)及[停止状态](stopped_status.json)作为不可变证据保留。用户运行后，继续在本地run的`session_recoveries`下保存每次重登的原因、原响应及其哈希。

# BaoStock第368项断线与Windows锁文件归档修复

用户在成功完成367/690项（326行）后，第368项`sh.601808 / query_performance_express_report`于2026-09-07 13:15:03 UTC遇到连接提前关闭，SDK返回10002007网络接收错误，无数据。随后异常收尾的文件哈希扫描读取包装进程持有的`.resume.lock`，触发Windows PermissionError，导致manifest没有更新。再次启动读取旧清单，才出现`login.json`不匹配；不是又一次登录拒绝的证据。

已在本机用父进程持有FileLock、子进程读取同一文件复现PermissionError。旧清单三处差异（login、status、第100项原失败响应）都在上次session_recoveries/0001中找到相同哈希原件；其余106项旧清单条目一致。原10001001的恢复已经实际成功，不能继续称该恢复未经实测。

修复`bc25efe`：归档只排除运行时锁文件，不吞掉真正证据文件的读错误；收尾失败记录manifest_error，但不掩盖原网络异常。对两个只读事件接口、明确空10002007响应增加有界重连：每项最多两次，15/30秒退避，全批最多20次接收错误恢复，纳入已有60次总预算。10001001每项仍最多一次，黑名单10001011、部分数据、未知错误、失败登录与预算超限仍停止。连接关闭不能证明服务端没收到请求；允许重放是因为这两个请求只读，不意味着将不确定响应标记为成功。

单次离线修复保存原manifest、状态、登录、绑定和脚本/策略快照，再明确记录代码绑定修订；原数据配置与成员输入哈希未改变。367项成功记录逐项核对计划、代码、字段宽度与326行合计；268项此前未封存成功记录首次取得哈希，不冒充此前已有独立哈希证明。所有368份响应文件字节未改，失败第368项仍保留，等待用户执行时再归档并重发。

12项针对性测试通过，包括真实持锁下manifest生成、实际证据读失败不掩盖原异常、成功采集不能隐藏归档失败、会话/网络恢复预算、只读限制及既有事件范围检查。`--check`核对367项和新manifest通过。助手没有发出任何新网络请求、没有重启采集；状态仍STOPPED，剩323项待用户使用同一续采指令运行。研究准入及2021–2025封存均不变。

[离线修复证据](repair.json)、[失败响应](failed_response.json)、[停止状态](stopped_status.json)。完整修复快照在本地`experiments/r2/r2_baostock_event_bulk_v1/repairs/20260907_transport_lock_v1`。

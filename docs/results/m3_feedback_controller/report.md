# M3 model-stage feedback and finite research loop

The existing Alpha34 model NO_GO is now available through campaign memory and
refinement feedback. Its original IC_SCREEN_PASS remains an earlier stage. The
model evidence was imported read-only from `20260907T030012969333Z` after checking
the artifact manifest, candidate identity, screen lineage and numerical registry.
It is explicitly LEGACY_VERIFIED_IMPORT because that model run preceded the new
controller model reservation. Repeated attachment produced identical state.

The real zero-evaluation duplicate campaign was frozen with an empty selection.
It retained both duplicate attempts, performed NO_ENTRY with zero fits and zero
portfolios, and left the empirical registries unchanged: 28 M1/M3 evaluations and
33 M2 evaluations. The files here retain the live CLI evidence.

New controller capabilities:

- Immutable campaign freeze with the entire search family and original budgets.
- A separately predeclared, once-only model reservation; restart and failure do
  not reset it. Zero remains the default for old specifications.
- Cross-round candidate selection into matched BASE/ADD models, preserving each
  screen origin and rejecting name collisions before data loading.
- Append-only model-stage feedback and deterministic trajectory retrieval.
- A finite loop binding endpoint, brief, schema and parent-selection policy;
  previous-round parents are chosen by ID, and pending/failed calls stop execution.

The two-round loop test uses real HTTP transport with controlled response and
evaluator fixtures. It verifies crossover lineage, propagation of negative model
feedback, crash handling and no repeat requests after restart. It is not proof of
a real LLM generating hypotheses or a real multi-round research run. The new
cross-batch model dispatcher has targeted routing tests; the previous Alpha34
run separately verifies the full numerical kernel. No new fitting was performed
in this batch merely to reproduce that failed factor.

Regression: **215 passed**, five existing warnings. Real model inference,
upstream AlphaForge/AlphaSAGE generated assets, auditor and fundamental data
adapters remain unaccepted. Stored q-values remain historical exploratory
statistics, not an adaptive campaign-wide confirmation claim. Qualification and
lockbox remain sealed.

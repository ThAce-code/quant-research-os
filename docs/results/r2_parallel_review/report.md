# R2 轻量模型文书转录：90份完成，事件准入待继续

核对日期：2026-09-07。用户明确授权将文书处理分派给轻量模型；3个 `gpt-5.6-luna` 工作单元各处理30份，主Agent负责证据校验、返修与字段语义复核。实现提交 `e6f5e54`，run `r2_parallel_review_v1`。本批仅使用已归档原件，零网络请求、零收益读取、零因子实验。

## 实际交付

从原180份待内容审查文书中，按BaoStock待审映射优先级、公告日、证券代码和文书ID，选取前90份预告/预警类标题文书。标题只用于分派；报告期另以正文验证。每条保存原文页码、引文、来源URL、原件哈希、模型身份、观察时间及缺失项，见[90条转录](transcriptions.json)。未筛选收益或因子表现。

| 检查轮次 | 通过引用/数值检查 | 待修正 |
|---|---:|---:|
| 首轮 | 71 | 19 |
| 第二轮 | 89 | 1 |
| 第三轮 | 90 | 0 |

这不是模型准确率评测：返修包含模型引文/字段错误，也包含校验器对千元、百万元、人民币若干亿和点分日期的格式支持修正。保留每轮草稿和审计，不用最后90/90掩盖首轮问题。

净利润为70个区间、5个近似点值、15个缺失；同比为65个区间、8个近似点值、1个点值、2个开放下界、14个缺失。“约600万元”保留近似点值，不能变成最低600万元；“增长50%以上”只有下界。原文明确印出的0允许保留，未披露不填0。区间中的“约”等限定词仍在引文中，区间不是统计置信区间。

## 主Agent复核及保留问题

主Agent检查了90条当期净利润/同比的原文文本片段、列含义、正负方向和单位；未声称完成90份全文图像审查、落款语义核验或版本召回证明。自动PASS只说明本批实现覆盖的引用、格式和数值检查通过，不能替代完整业务验收。

- `1200833583` 同时给出调整前后基数，当前转录保留调整后同比；`1200826551` 同时给出已报/追溯调整口径，当前保留首表同比。两条均隔离于跨源比较，不能混合基数。来源和问题记录在各自转录中。
- `1202122795` 引用了2016-03-22前次预告。现有引用还没有在本批核验成原件→修正事件链，不回填过去数值。`1200833583` 引用的是会计差错更正公告，不能冒充预告修正链。
- 30条落款日期保留null；其余落款字段仍为模型源转录，未由主Agent逐一语义认证。来源索引公告日期另列保存，落款不得代替历史可用日。
- 各条均 `research_admission=false`。可用日、当日成员资格、版本先后、重复事件和BaoStock同口径映射留待事件表整合，本批没有改写原映射或声称覆盖全部版本。

## 当前进度与下一步

此前30份指定字段转录加本批90份，累计120份；原180份内容待审队列中还有90份未完成本批结构化转录。另有一份此前已隔离的未转录取消文书，因此这些数量不能解释为全部211份均有可用事件。新90份全部尚待正式事件表整合；既有28条事件表及其验收保持不变。

下一批优先把这90份接入期间/版本/公告次交易日/当日成员审查，并处理两处比较基数，再审查剩余文书（含年报内嵌预告、快报与标题误命中）。依覆盖情况决定能否冻结有限事件因子试验。当前尚未找到新Alpha、未启动模拟盘，2021–2025继续封存。

8项新unittest及13项既有R2针对性测试通过；亏损方向、点值冒充区间、日期/人民币亿格式均保留实际RED→GREEN证据，见[test_evidence.txt](test_evidence.txt)。旧424份公开证据哈希和M0身份通过，两库哈希不变（47/33条evaluation、KEEP0）。见[验证](verification.json)、[本批来源绑定](source_binding.json)。
